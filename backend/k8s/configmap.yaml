apiVersion: v1
kind: ConfigMap
metadata:
  name: guitarstore-config
  namespace: guitarstore
data:
  DEBUG: "True"
  DB_NAME: "guitarstore"
  DB_USER: "postgres"
  DB_PORT: "5432"
  DB_HOST: "postgres-service"
  CORS_ALLOW_ALL: "True"
  ALLOWED_HOSTS: "*"
